# Surface Lab — Pass 2 AC (expand design refs)

**Prior:** Pass 1 GO + Phil design-ref lock stands. Live: https://dev-storm-clockers.github.io/surface-lab/  
**Budget:** prefer **one** pass then STOP if Done green. Pages-first. Lilly only if Phil yes via CoS→Harness.

## Goal
Expand the design-ref gallery with **2–3 new public landing patterns**, **stronger 3D**, and **fix Pass 1 non-blocking nits**. Update design-refs pack after Quality STOP-GO (Phil already wants keep-going).

## New samples (pick 3 — fictional brands, distinct systems)
1. **Cafe / restaurant** — menu highlight, hours, reserve/contact CTA
2. **SaaS / waitlist** — product hero, 3 feature cards, email waitlist (no backend — UI only)
3. **Event / meetup** — date/venue, agenda strip, RSVP CTA (UI only)

Must not look like Pass 1 services/creator/shop clones (new palette/type/layout per page).

## Stronger 3D
At least **two** of the new samples (or one new + upgraded Pass 1 page) must show a **clearly stronger** 3D treatment than Pass 1 light tilt — e.g. interactive CSS 3D flip/stack, parallax layered depth, or a small WebGL/three.js hero that actually renders (if labeled WebGL, it must be WebGL — no false labels).

## Nit fixes (Pass 1)
1. Shop sample **name consistency** (ARTIFACT/docs vs live page title match).
2. Any **WebGL vs CSS** mislabel fixed (technique notes + gallery tags accurate).

## Hard stops (Verifier)
1. Gallery lists **Pass 1’s 3 + new 2–3** samples with working links.
2. Each new sample is a distinct mobile-first landing matching cafe / SaaS-waitlist / event patterns above.
3. ≥2 stronger-3D treatments visible and correctly labeled.
4. Pass 1 nit fixes verified (name consistency + technique labels).
5. NightDeck / Dungeon Dad untouched.
6. ARTIFACT.md updated (paths, techniques, pass 2).

## Must-holds
- Original fictional brands only
- Ship to Pages `dev-storm-clockers/surface-lab` (+ design-refs sync after GO)
- Prefer STOP after this pass

## Out of scope
Auth/payments/real email · CMS · NightDeck/Dungeon Dad · endless taste · high-effort stacked Codex without Phil yes
